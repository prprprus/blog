<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.springbootstaging.mapper.PeopleMapper" >
    <resultMap id="BaseResultMap" type="com.example.springbootstaging.model.People" >
        <id column="id" property="id" jdbcType="BIGINT" />
        <result column="name" property="name" jdbcType="VARCHAR" />
        <result column="age" property="age" jdbcType="INTEGER" />
        <result column="sex" property="sex" javaType="com.example.springbootstaging.enums.PeopleEnum"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, name, age, sex
    </sql>
    
    <select id="getALL" resultMap="BaseResultMap">
        SELECT
            <include refid="Base_Column_List"></include>
        FROM
             people
    </select>

    <select id="getOne" parameterType="java.lang.Long" resultMap="BaseResultMap">
        SELECT
            <include refid="Base_Column_List"></include>
        FROM
            people
        WHERE
            id = #{id}
    </select>

    <insert id="insert" parameterType="com.example.springbootstaging.model.People">
        INSERT INTO
            people
            (name, age, sex)
        VALUES
            (#{name}, #{age}, #{sex})
    </insert>

    <update id="update" parameterType="com.example.springbootstaging.model.People">
        UPDATE
            people
        SET
            <if test="name != null">name = #{name},</if>
            <if test="age > 0">age = #{age},</if>
            sex = #{sex}
        WHERE
              id = #{id}
    </update>

    <delete id="delete" parameterType="java.lang.Long">
        DELETE FROM
            people
        WHERE
            id = #{id}
    </delete>

</mapper>
